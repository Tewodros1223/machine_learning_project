# Frontend

Minimal React frontend scaffold. To run:

```bash
cd frontend
npm install
npm start
```

The frontend includes simple pages for registration, login, camera face capture, and a quiz stub. Update API endpoints to point to the backend (default: http://localhost:8000).
